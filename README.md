# Digital Marketing Monthly Revenue Predictor

A Streamlit app that estimates campaign-attributed monthly revenue from campaign planning inputs.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

The saved model (`model.pkl`) and feature metadata (`feature_metadata.json`) are included so the app can run immediately.
